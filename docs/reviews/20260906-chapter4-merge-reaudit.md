# 第四章合并复查与恢复验收

本轮针对用户报告的三处扩展空间、校友墙和最终“灿若星辰”两问，比较 `a1c087d`（9 月 5 日已发布版本）、`dfe434d`（9 月 6 日初次整合）、`dc71378`（剧情恢复）、`f415e74`（验证器改动）及 `f3f3353`／PR #49 合并后的代码。以下修复在本地 `integration-20260906`，尚未提交或推送。

## 确认的遗漏

| 范围 | 历史依据与实际问题 | 本轮恢复 |
| --- | --- | --- |
| 三处墙后扩展空间 | `a1c087d` 把三处实体边界移到 264、248、584，并以同源前景裁片显示局部身体淡化；`dfe434d` 留下几何但漏掉消费逻辑 | 补回脚点与前景深度同步、22% 身体可见效果；补齐 `playerRevealAlpha` 传递及 `__BASE` 裁片。人物回到共享深度合同 |
| 主入口门与边缘约束 | 门运行时只剩字段；完整人物边界约束也没有接回 | 补回门叶、门障碍及生命周期；完整底图均使用 `__BASE`；切层同时更新物理世界和人物视觉边界 |
| 校友墙 | 20 位人物被统一使用 A3 偏移和低深度，缺幂等创建及点击热区 | 按人物自身 A1／A3 坐标和墙面深度绘制，保留近距离 Space，资源刷新不会重复创建 |
| 最终两问 | `ChapterFourExteriorQuestions` 文件尚存，但 `dfe434d` 替换了实际挂载；原逐题浮现、消散样式也丢失 | 当前 `Host → StarLampClosure → ExteriorQuestions` 复用原逐题组件及粒子样式；两项原子保存后才点灯；修复手机横屏容器中的选项裁切 |
| 维修推车 | 独立车体被整张动作图替代，人物裁切、翻转及分层衔接没有消费 | 按历史参数恢复人物专用裁切、车体共同位移和动作切换 |
| 签到设备 | 像素读卡器与纸槽退回纯色矩形 | 恢复两种设备图形及接受道具后的状态 |
| 配电成功呈现 | 逐区脉冲与 1320 ms 收束丢失 | 补回呈现与清理；呈现期间不步进追逐模型，不消耗 2 秒宽限 |
| 验证保障 | `f415e74` 把三份完整验证器缩为短脚本，失去控制器、读档、身份与完成证明检查 | 以 `e07a72e`／`a1c087d` 为主体、以 `f415e74^` 的最终灯会话测试补足，恢复完整行为矩阵，并保留后加补充检查 |

三层静态碰撞、可行走区域和前景遮挡的几何，与 `a1c087d` 逐项相同；没有重新绘制墙体，也没有更换三层地图或布局 JSON。新版四组家具、四关 Three.js 楼梯、独立追逐楼梯、2 秒／174 追逐、二楼主动关门、原始分层星灯与短播放时序继续保留。电网锁定事件不再携带与其职责无关的空朱答案字段。

## 实际浏览器验收

三处墙面均以原生键盘从大厅走到目标，经过墙前、局部遮挡、底边阻挡，再绕到墙面前侧。没有直接改写人物坐标或提交剧情完成事件。

| 位置 | 实测脚部底边 | 覆盖与恢复 |
| --- | --- | --- |
| 面包坊北墙 | 264 | 墙后裁片透明度 0.78，保留约 0.22 的被覆盖身体；前侧恢复不透明正常层级 |
| 104 教室北墙 | 248 | 同上，保留既有黑板和家具阻挡 |
| 104／105 横墙 | 584 | 同上，绕教室门洞后正常显示 |

上述三条路径在 Chrome／Firefox 1063×739、WebKit 390×844 均通过，页面错误为零。路径脚本最初在较窄走道和家具边缘取点不适合时失败；修正导航余量与门洞路线后重新从大厅走完，未把失败运行计作通过。

两问在 Chrome 1280×720、Firefox 1063×739、WebKit 390×844 实际点击完成：第一问后两项持久答案仍为空；第一问消散后才显示第二问；第二问结束后一起保存所选 ID；“回答已保存”后播放原始分层灯光，最终确认进入 `complete` 且真实答案保留。手机验收另检查三个选项均完整位于 RPG 容器内，无文档横向溢出。WebKit 初稿能被自动化点击但存在选项裁切，已修正样式后重新验收。

Chrome 另从停电检查点原生键盘走到配电箱，实际按东走廊、教室区、面包店后场完成三次切换。配电完成呈现期间追逐模型消耗时间为 0；呈现结束后，守卫直到追逐模型约 2016.6 ms 才出现，确认 2 秒宽限没有被演出占用。

## 程序验证与边界

- 恢复的控制器／存档运行时矩阵：1179 条主体断言通过，另执行历史恢复、场景、追逐楼梯和保安补充检查。
- 任务、DEV 存档隔离、单一 Scene producer 与空间证明矩阵：408 条断言通过。
- 场景恢复检查：28/28；具体呈现方法执行检查：7/7，覆盖分层推车、配电暂停追逐、设备图形、墙面前后切换和完整帧传递。
- `npm run validate:release`：35/35 通过，含文本索引、资源、控制器、三层拓扑、生产构建、离线单文件生成与三条浏览器烟测。离线产物为本地 `demo/index.html`，241901608 字节；没有手工编辑。
- 进一步静态比较第四章之外 37 个既有 TS／TSX 文件的删除方法、函数和组件挂载；除已明确替换的剧场、骑行、钓鱼机制和已知两问入口外，未再确认新增遗漏。该结论不代表全游戏连续通关。

维修推车、设备绘制的本次新增证明来自真实方法的定向执行测试；没有把它们写成整段人工通关证据。浏览器专项和程序矩阵共同用于本轮验收，不能仅以通过项数量推导所有视觉效果正确。

## 直接对比 9 月 5 日完整版本

按用户追加要求，再以 `a1c087d4d7b9a503c8b50f4ea72a2c7b5ab118c9`（`demo-20260905`）直接对比当前本地工作树，包含所有尚未提交的恢复改动；不以 9 月 6 日中间合并稿作为正确基准。JSON 逐字段比较忽略键排列，场景方法比较忽略注释与空白。

这一轮直接比较额外找出并恢复六处遗漏：

1. `snapshotBackgroundTextures`：恢复只保存已加载楼层的快照，避免只需 A1 的入场阶段被尚未加载的 A2／A3 阻断。
2. `validateFrameRegistrationReport`：只有全部精灵表加载后才检查完整帧总数；部分加载仍检查已注册部分的内部一致性，完整加载后的损坏仍会报告。
3. `ensureMaintenanceRuntime`：隐藏旧的现场拾取撬棒、单独捡油、单独修钟齿轮视觉，避免提示玩家操作控制器已经停用的入口；诊断给撬棒、开轮罩得油的现行事务不变。
4. `redrawAlumniPanel`：恢复 760×420 资料卡、150×232 肖像、长角色介绍和来源换行、返回地图按钮。Chrome 原生移动后按 Space 打开程开甲资料，确认 20 张肖像只创建一次，并按 Esc 关闭。
5. `createHud`：恢复共享像素字体与 1 px 描边，去掉回退版本的 5 px 粗描边和额外阴影。
6. `createMaintenanceGuardRuntime`：重建维修保安时同步初始化呈现状态，避免旧一轮的呈现状态遗留。

新增两个真实方法执行探针：仅 A1 加载时的快照、部分精灵加载与完整加载损坏的区别。前者在修复前实际抛出 `background_missing:2`，修复后两个均通过，呈现探针合计 9/9。

比较后的第四章场景有 256 个方法，9 月 5 日有 248 个；其中 200 个方法的代码 token 完全相同，44 个方法有差异，12 个是后加的方法。未保留的四个旧方法分别由新证据绑定渲染与四组家具操作取代：`createEvidenceDetailMark`、`updateRoom204CarryGhost`、`resolveNearbyRoom204PieceId`、`selectRoom204Piece`。该计数是定位差异的依据，不作为功能覆盖率。

当前布局 JSON 相对 9 月 5 日仅有四项逐字段差异：204 叠合后的状态文案、追逐速度 196→174、移除旧 A2 入场停顿、增加 2000 ms 起步宽限。三层墙体、可走区域、前景裁片、人物墙后淡化字段、校友数据均没有几何改动。主线内容 JSON 的剩余差异集中于独立追逐楼梯任务、三步配电规则和新追逐状态机；没有据此回退后来明确采用的玩法。

## 面包坊柜台注释后的实际流程修复

历史代码一致和状态模型通过仍不足以证明柜台上的物件可见。用户随后标出的区域存在确实的覆盖问题：柜台裁片深度4387，高于传送带4348、检查灯4342和时针4347。柜台会把正在运动和变化的物件重新盖住。现在三者及配套轨道、托盘、闪光以柜台表面为共同深度基准，保持原始位置与所有碰撞。

完整按键与拖拽路径还发现回大厅后的插槽无法启用：连续A1地图的步行不改写故事位置，记录仍是 `a1_bakery`，原插槽只接收大厅位置别名。该别名现已加入插槽合同，实际距离与模式仍为硬校验；新增控制器回归先复现 `locked` 而非 `too_far` 的错误，再验证远处零写入、靠近安装、读档仍待手动调钟。

Chrome、Firefox及手机尺寸WebKit均实际完成带面显示、模式切换、停带、时针露出与拾取、原路回大厅、道具栏拖入旧钟、手动转到18:50。浏览器没有直接改写玩家坐标和故事事实。WebKit道具栏覆盖模式按钮的问题也已修正并复验。核心发布套件35/35通过，手机CSS调整另行浏览器验证并重建离线单文件。此前仅验证 `bakeryConveyorMotionActive` 和拾取事实的证据，不再用来声称其视觉显示正确。
