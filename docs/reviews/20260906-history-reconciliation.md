# 2026-09-06 集成版本历史核对与恢复

本轮确认此前集成提交 `dfe434d` 覆盖了已经进入历史的新内容：新站立帧仍在，侧向走路帧退回旧版；第四章丢失了一批已经实现的交互与展示。问题主要是合并后的文件内容回退，不能用“提交已经是 main 的祖先”证明功能完整。本轮按功能恢复，工作目录为 `integration-20260906`，基于 `f70b7ef`；修改尚未提交、推送。

## 历史核对范围

- 核对本地分支、远端引用及其祖先关系，并检查 8 个有效工作树；另有一个失效 `.git` 的第四章交付目录，只作为文件快照。
- 两个本地未被远端引用直接包含的提交 `ae375c9`、`0358624`，补丁内容分别对应已发布的 `d7d0e3d`、`f70b7ef`。其他被核对分支的提交均已进入远端历史。
- 枚举 7 个 stash。9 月 4 日的最近备份中主要功能后来已发布；其余是 7 月至 8 月的旧备份。本轮没有把全部 stash 每一行都认定为待恢复功能。
- `delivery-20260826` 中存在未提交的认证模型、地图约束、任务提示和基础 UI 实验，缺乏作为正式方案的提交依据，未自动并入。旧启真湖草案、30 秒钓鱼图谱、宣传片实验也未覆盖当前实现。

## 已确认的恢复清单

| 内容 | 历史依据 | 本次处理 |
| --- | --- | --- |
| 角色侧向行走八帧 | `53e3121` | 恢复八张原始 PNG，保留当前新站立帧和构建器；恢复素材说明 |
| 大厅旧钟手动调节 | `672f3f6`、`e07a72e` | 恢复回到旧钟后选择刻度、确认才改变时间；存档保留待调钟状态 |
| 五边形配电面板 | `866ae84` | 恢复五节点连线布局；保留当前初始掩码 6、目标 13、最多三次切换规则 |
| 面包房传送带 | `672f3f6` | 恢复动态带面、载物、指示灯、减速停止和时针取回；柜台人员保持独立 |
| 六组调查谜题 | `8a46dd4` | 恢复场景入口、Host 覆盖层、控制器奖励及存档事实；包括值班牌、档案索引、影像对齐、定位校准、电力拓扑、疏散路线 |
| 电梯楼层记录与停靠链 | `0e548ae` | 恢复三段楼层记录与运行复核，保留当前 A3→四关楼梯→A2 因果门禁 |
| 维修诊断 | `ac30d19` | 恢复诊断面板；诊断取得撬棒，打开轮罩后才取得润滑油 |
| 环境交互与 204 修复后展示 | `156d987` | 恢复环境入口及后续阶段持续展示；保留当前原始证据标记与回声生命周期 |

今日已确认的新实现继续保留：四关 Three.js 错位楼梯、独立追逐楼梯间、2 秒追逐宽限、保安路径与捕获对白、二楼关门动作、原版层叠星灯和最终两问、四种渐进钓鱼节奏。未重新引入无尽模式。此前工作树中的低电量恶作剧、捕获生命周期修复、无关宣传片文件保持各自范围。

## 验证证据

- `npm run build`：通过 TypeScript 检查及生产构建；存在既有体积提示，无构建错误。
- `npm run verify:rpg-character-sprites`：完整人物、八帧侧行、站立帧与透明边界检查通过。
- `chapter4:validate-effective-interactions`：624 条断言通过。
- `verify-chapter4-restored-scene.mjs`：26/26 通过，覆盖恢复点及原始证据标记清理。
- `verify-chapter4-history-restoration.mjs`：两次待调钟存档、六组调查奖励与存档、重复提交拒绝、诊断→轮罩→润滑油、204 后续展示通过。
- `chapter4:validate-chase-stairwell`：顺序楼梯门禁、存档、本层失败重试、过期请求拒绝通过。旧断言改为分别检查 A1 与 A2 的安全重试点。
- `chapter4:validate-stair-campaign`：5,616 组视角/配置及两关高级乘台路线通过。
- `chapter4:validate-star-lamp`：189 条断言通过。
- 捕获生命周期、低电量恶作剧、四种钓鱼正常/辅助模式的确定性回放检查通过。
- Chrome 1280×720 实际键盘操作：从停电检查点走到配电箱并打开五边形面板；从大厅沿标注门洞路线走进面包房，检查指示灯、停止传送带并取得时针；走到大厅旧钟，打开调钟界面、选择 18:50 并确认时间变化。上述场景检查无 pageerror。另从大厅绕到柜台后方，实际打开了值班牌重建面板、调整顺序并提交成功，同时断言面板期间世界处于暂停状态；柜台前侧最近目标为值班助理。
- 完整关键验证最初暴露出旧验证器仍要求 1.2 秒／196 追逐和八段转场。已将其迁移到当前合同：2 秒／174、九段转场（五次时间覆盖、四次场景内交接），并恢复了随实际守卫距离切换的追逐音乐事件；`npm run validate:critical` 现为 24/24 通过。

截图与构建、浏览器日志位于 [验证目录](20260906-history-reconciliation-evidence/)。[配电面板](20260906-history-reconciliation-evidence/power-panel.png)、[面包房](20260906-history-reconciliation-evidence/bakery.png)、[手动调钟](20260906-history-reconciliation-evidence/clock.png)。

本轮浏览器验证为上述 Chrome 定点路径；未将它等同于全游戏通关、九种浏览器/视口组合验收或所有历史草稿逐行审定。`demo/index.html` 未在本轮重新生成或发布，当前恢复结果由 4183 开发预览提供。
